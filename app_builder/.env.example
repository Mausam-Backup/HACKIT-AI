# Dashboard port (default: 4097)
DASHBOARD_PORT=4097

# Opencode server port (default: 4096, config.yaml overrides this)
# OPENCODE_PORT=4190

# OpenRouter API key (not required for local models)
# OPENROUTER_API_KEY=sk-...
